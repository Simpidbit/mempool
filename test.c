#include <simpool.h>
#include <stdio.h>

int main()
{
    printf("Hello\n");
    /*
    simpool_t * pool = create_simpool();

    simmem_t * m1 = malloc_simpool(pool, 100);
    simmem_t * m2 = malloc_simpool(pool, 20);
    free_simpool(m1);
    simmem_t * m3 = malloc_simpool(pool, 300);
    free_simpool(m2);
    simmem_t * m4 = malloc_simpool(pool, 10);
    free_simpool(m3);
    simmem_t * m5 = malloc_simpool(pool, 256);
    free_simpool(m5);
    free_simpool(m4);

    terminate_simpool(pool);
    */
    return 0;
}
